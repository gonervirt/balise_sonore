version: '3'
services:
  frontend:
    build:
      context: ..
      dockerfile: tests/Dockerfile
      target: frontend
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build:
      context: ..
      dockerfile: tests/Dockerfile
      target: backend
    ports:
      - "5000:5000"
